import 'package:flutter/material.dart';

class CamTest extends StatelessWidget {
  const CamTest({
    Key? key,
})  : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}